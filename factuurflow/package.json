{
  "name": "factuurflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Automatische inkomende factuurverwerking voor Moneybird en Exact Online",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "samples": "node dist/samples.js",
    "extract": "node dist/cli.js",
    "serve": "node dist/server.js",
    "test": "node dist/test.js",
    "dev": "npm run build && npm run test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
